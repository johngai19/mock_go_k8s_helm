api-service-beta:
  replicaCount: 3
  config:
    apiBasePath: /beta/v1
worker-beta:
  resources:
    requests:
      cpu: 100m
      memory: 64Mi
global:
  environment: development
  umbrellaName: appstack-beta
  default_registry: '@{REGISTRY}'
